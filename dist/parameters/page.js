// Regular application parameter: page
// Validate at the application boundary and again before sensitive use.
export const pageParameter = {
    name: "page",
    required: false,
    maxLength: 256
};
